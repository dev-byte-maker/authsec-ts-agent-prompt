{
  "name": "authsec-ts-agent-prompt",
  "version": "1.0.0",
  "description": "AuthSec-protected MCP server for agent prompt management",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "node --env-file=.env --watch dist/index.js",
    "dev:build": "tsc --watch"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": ["mcp", "authsec", "oauth", "typescript"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@authsec/sdk": "^4.4.3",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^22.0.0",
    "typescript": "^6.0.3"
  }
}
